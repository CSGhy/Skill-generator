# 2026-09-04 工作记录

## 审查用户通过 Trae IDE 对 SKILL 的改动

用户于 09:43–09:45 改了 5 个文件：`_shared/standards.md`、`_shared/validate_test_cases.py`、
`test-case-generator`、`test-case-xinchuang`、`test-case-execution-helper`。

### 用户改动内容（方向正确，但迁移未做完）
- 测试类型由中文改为**英文枚举**（functional/boundary/equivalence/negative/...），对齐 standards.md 五、
- xinchuang 补 GB/T 标准号（SM2: GB/T 32918-2016、SM3: GB/T 32905-2016、SM4: GB/T 32907-2016、国密TLS: GM/T 0024-2014）
- execution-helper 补路由规则、偶现转缺陷关单条件（连续 ≥3 轮未复现）、与 report-generator 数据传递

### 发现的问题（详见 SKILL_REVIEW_2026-09-04.md）
- **P0-1 校验链断裂**：英文枚举只改了 standards + validator，下游 10 个 SKILL 共 118 行仍在用中文测试类型。
  昨天 PASS 的 demo_login_cases.md 今天 FAIL：「场景覆盖: 仅覆盖 0 类必覆盖场景（set()）」。
- **P0-2 standards.md 自相矛盾**：第 75/99 行示例仍为「功能测试」（中文），第 110-125 行枚举表为英文；
  九、必覆盖场景类型仍中文且未与英文枚举建立映射。
- **P1-3** validate_case 未逐条校验「测试类型」枚举，中文值静默通过、只在批量规则以「0类」报错，无法定位。
- **P1-4** test-case-generator:31/54 的 `--type` 仍含 security|performance，而 generator-core:29 已移除 → 主入口与 core 矛盾。
- **P1-5** xinchuang / execution-helper 版本历史有 v1.0.1(09-04) 但 frontmatter 未同步；generator 改动无版本记录。
- **P2-6** multi-agent-test-auditor/prompt-template.md:4 断链（`../skills/...` 应为 `./SKILL.md`），全库 436 引用中唯一 1 处。
- **P2-7** lanhu-requirements-doc / multi-agent-research-prover / multi-agent-test-auditor 缺 version frontmatter。

### 已验证无问题
validator 自测/反向测试/`.md` 解析均正常；无旧 ID 残留；无已移除 SKILL 的残留引用；
feedback.json / trace.json 路径一致；16 个 junction 注册完好。

### 关键教训
`.trae` 是 git 仓库（HEAD: a377f21），但 `skills/_shared/`、`test-case-*` 多为 untracked，无法直接 diff；
定位"谁改了什么"要靠 **文件 mtime**（`find -newermt`），这是本仓库的有效手段。

### 产出
- `D:\Program Files\Code\AiSkill\.trae\skills\SKILL_REVIEW_2026-09-04.md`（含 P0/P1/P2 分级与 A/B/C 三套修复方案）

---

## 方案 C 执行完成（2026-09-04 下午）

用户确认按方案 C 修复。全部落地并回归通过。

### 关键修正点
- standards.md 第 75/99 行示例 `功能测试`→`functional`；九、补中文↔英文场景映射。
- validator 加**中文→英文归一化兜底**（中文值转英文+WARN）+ **逐条测试类型枚举校验**（非法值定位到用例 ID）。
- 迁移脚本漏网：原只匹配 JSON 风格 `"测试类型": "功能测试"`，漏掉**表格单元格值**。补改 generator-core 示例表 7 处单元格 `功能测试/边界测试/等价类测试/负向测试/安全测试`→英文。
- `--type` 枚举矛盾：generator 移除 security/performance（与 core 一致）。
- 版本元信息：xinchuang/execution-helper → 1.0.1；generator 补 v3.3.1。
- 断链 P2-6 修复（prompt-template.md → ./SKILL.md）。
- 两个真 bug：xinchuang 把测试类型 `compatibility` 误标为策略家族 → `equivalence_boundary`（generator:444 散文同步）；core 示例 LOGIN_004 测试场景仅 3 段（标准要 4 段）→ 补「锁定15分钟」段。

### 回归结论（已实测）
- validator 自测 PASS(0/0)；英文标杆产物 PASS(0/0)；中文兜底 PASS(0 错 + 4 归一化 WARN)。
- 值位置中文测试类型残留 = 0；非法策略家族误用 = 0；全库断链 = 0。
- 临时脚本 `_migrate_test_type.py` / `_audit_skill_examples.py` / `__pycache__` 已清理。

### 遗留（已知、非阻断）
各 SKILL 内置示例用例表为教学性少行 demo，不满足批量硬门槛（家族多样性/金字塔/场景覆盖），属示例性质，不在方案 C 范围。

### 关键教训（追加）
- **迁移校验请用"值位置"而非"全文命中"**：中文测试类型在 SKILL 里有 JSON 键值（`"测试类型": "功能测试"`）和**表格单元格**（`| 功能测试 |`）两种形态，替换脚本必须同时覆盖，否则会漏。
- 审计脚本对 api-generator 的"字段定义表"有**误报**（把 字段/说明/必填/示例 表当作用例表），属审计工具局限，不影响结论。

---

## 盘点外部测试 SKILL 并新增 5 个（2026-09-04 下午）

用户问 GitHub/Gitee/SkillHub 上还有无好用的软件测试 SKILL。盘点结论：原 16 个已覆盖功能测试生命周期，缺口为 测试数据生成 / 小程序自动化 / 专项性能(k6,PTS) / UI 自动驱动。

从 SkillHub 安装 5 个补齐（real 目录在 `.trae/skills/`，junction 同步到 `.workbuddy/skills/`）：
- `qa-test-data-gen`（中文测试数据生成）
- `weapp-automated-testing`（微信小程序自动化；上游 name 字段有 typo `weapp-automatd-testing`，未改）
- `performance-testing-toolkit`（HTTP 压测/负载/基准）
- `alibabacloud-pts-ops`（阿里云 PTS 云端压测）
- `playwright`（Playwright 浏览器自动化/MCP）

已更新 `SKILLS_REGISTERED.md`：记录新增 + "后续新增技能规范"（真实文件放 `.trae/skills`，再在 `.workbuddy/skills` 建 Junction）+ SkillHub 下载命令。

### 关键教训（沙箱坑）
本沙箱 `curl -o file` 会报 write error 23（0 字节）；必须改用 shell 重定向 `curl ... > file` 才能写盘。下载 SkillHub 技能包用：`curl -L -s "https://lightmake.site/api/v1/download?slug=<slug>" > /tmp/<slug>.zip` 再 python zipfile 解压。

---

## 编写换电脑迁移清单（2026-09-04 下午）

用户问 `.workbuddy/skills` 里一堆 SKILL 是什么、换电脑能否直接用。盘点后写 `SKILLS_MIGRATION_GUIDE.md`。

### 家底结论
- `.workbuddy/skills` 共 28 条目 + 1 配置文件（`_bm_skillid_migration.json`，可忽略）。
- **A 类（Junction 链接，真身在 `.trae/skills`，是 git 仓库 `Skill-generator`，remote: https://github.com/CSGhy/Skill-generator.git，branch main，HEAD a377f21）**：22 个（含 `_shared` + 测试套件全家桶 + 5 个新装 SkillHub 技能）。
- **B 类（真实文件夹，独立安装）**：5 个 —— `attendance-ledger`、`law-skills__skillhub`、`pwa-jsdom-test`、`pwa-playwright-windows-testing`、`pwa-to-apk`。内部未写死本机路径，可拷走用。

### 迁移要点（写入文档）
- A 类：新机 `git clone` 真身 → 跑 §4 PowerShell 脚本建 Junction（22 个名称列表已固化在文档里）。`../_shared` 相对引用自动成立。
- B 类：来源重装（law/pwa 走 SkillHub；attendace-ledger 走 `D:\考勤台账\tools`）或整文件夹拷贝 + 配依赖（Python/Node/Java+Android SDK/Playwright 浏览器）。
- **禁忌**：别 `robocopy /SL` 拷整个 `.workbuddy/skills`，会带死链；Explorer 拖拽（跟随链接复制内容）或逻辑迁移最稳。
- 收尾：重启会话 → 抽查 `/test-case-generator`、`/qa-test-data-gen`、`/playwright` 能调即完工。

### 关键教训
`.trae/skills` 既是技能真身仓库又是 WorkBuddy 标准扫描源（经 junction）。换电脑的核心风险是"链接指针"而非"文件"，所以迁移本质是"搬真身 + 重建链接"，不是拷 `.workbuddy/skills`。

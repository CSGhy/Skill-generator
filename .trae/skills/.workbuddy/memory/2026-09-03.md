# 2026-09-03 工作日志

## SKILL 体系优化（用户指令：保留 multi-agent-research-prover，清理其余两个外来 SKILL，其余按建议修改）

### 清理
- 将 `skill-cleaner`（Codex/OpenClaw 工具，不属测试域）与 `WorkBuddy`（加油站打卡，无关）移入 `.trae/_removed_skills/`（可逆，非硬删）。
- 保留 `multi-agent-research-prover`（数学证明，用户要求先不动）。

### 新增能力（补齐用户画像缺口：9 年手工功能测试 + 信创/国产化方向）
- `test-case-xinchuang`：信创/国产化/政务适配生成器，ID `XC_{模块}_{序号}`，覆盖国产 OS/DB/MW/浏览器/CPU、国密 SM2/3/4、等保/密评、政务专网。已在主入口 v3.3.0 注册。
- `test-case-execution-helper`：手工测试执行助手（record/session/patrol/repro/report），已在主入口 v3.3.0 注册。

### 落地的关键修复
- P0-4 旧编号：knowledge-base / api-generator / automation-guide / reviewer / report-generator 全面改为公共标准 ID 格式（LOGIN_001、API_LOGIN_001、BUG_ORDER_001 等）。
- P0-1/P0-2 缺失 references：新建 `security-generator/references/{owasp-top10,payload-library}.md`、`defect-manager/references/defect-templates.md`。
- P0-3 validator 打通：`_shared/validate_test_cases.py` 增强为可解析 .md/.csv/.json 实际产物，core（阶段5 强制调用）+ reviewer（可选调用）已接线，自测通过。
- P2-9 self-improving-helper 真实闭环：新建 `_shared/feedback/feedback.json`（entries[] + lessons[]），新增「反馈存储与闭环协议」章节，定义 read-before-generate / write-after-generate / report 三步。
- P2-11 defect 存储路径：由脆弱的 `_shared/defects/trace.json` 改为项目级 `.workbuddy/test-trace/trace.json`（自动建目录、跨会话持久），已预建文件。
- P2-12 参数/枚举一致性：core `--type` 移除不支持的 security/performance（转专门 SKILL）；`--coverage` 明确为「需求点覆盖率（非代码覆盖率）」并定义 100/80/60/40 含义；api-generator `--test-coverage` 统一改名为 `--coverage`。

### 版本号
- test-case-generator → 3.3.0；self-improving-helper → 1.2.0；defect-manager → 1.1.0；generator-core → 2.2.0；api-generator → 1.2.0。

## 让技能「可被 WorkBuddy 调用」（用户问：可以修改存放路径吗）
- 原目录 `.trae/skills/` 不在标准发现路径，无法直接 `/调用`。采用**目录 junction**（非移动，避免相对引用断裂）。
- 在用户级标准路径 `C:\Users\Administrator\.workbuddy\skills\` 下为 16 个 skill + `_shared` 各建 junction；并为 `../../rules` 引用在 `C:\Users\Administrator\.workbuddy\rules` 建 junction 指向 `.trae/rules`。
- 用 PowerShell `New-Item -ItemType Junction`（Bash 的 `ln -s` 在 Windows 需特权被拒；`cmd /c mklink` 被 Bash 安全策略拦截）。已验证跨链读 SKILL.md、`../_shared`、`../../rules` 均解析正常。
- 注意：需重启/刷新 WorkBuddy 会话，加载器才会重新扫描并让这些技能出现在可调列表。还原 = 删除这些 junction（只删链接不动原文件）。
- 详见 `D:\Program Files\Code\AiSkill\.trae\skills\SKILLS_REGISTERED.md`。

### 端到端验证（用户"试一下"）
- `Skill` 工具成功从 junction 路径拉起 `test-case-generator`（无需重启会话，当前会话即生效）。
- 产出 6 条登录用例 `.md`（覆盖 功能/负向/边界 3 类场景 + equivalence_boundary/property_based/state_model/attack_surface 4 类策略家族），经 `_shared/validate_test_cases.py` 校验 **PASS（0 错误）**。
- 证实 P0-3"生成→校验闭环"在真实产物上跑通。产物：`C:\Users\Administrator\.workbuddy\demo_login_cases.md`。

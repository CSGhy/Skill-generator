---
alwaysApply: false
description: "生成、编写、评审测试用例时自动生效，包括功能测试、接口测试、单元测试等各类测试用例"
---

# 测试用例质量规范

## 一、必含要素

每条测试用例必须包含以下字段：

| 字段 | 说明 |
|------|------|
| 用例ID | 唯一标识，格式：`模块缩写_序号` |
| 测试场景 | 简要描述测试的业务场景 |
| 前置条件 | 执行用例前必须满足的条件 |
| 操作步骤 | 1、2、3... 按步骤描述操作动作 |
| 预期结果 | 每个步骤对应的预期输出，必须明确、可验证 |
| 优先级 | P0/P1/P2/P3 |
| 测试类型 | 功能/边界/异常/性能/安全等 |

## 二、设计原则（FIRST）

- **Fast（快速）**：用例执行要快，避免不必要的等待和依赖
- **Independent（独立）**：用例之间互不依赖，可单独执行，执行顺序不影响结果
- **Repeatable（可重复）**：多次执行结果一致，不依赖外部不稳定因素
- **Self-validating（自验证）**：有明确的断言/预期结果，不需要人工判断
- **Timely（及时）**：需求确定后及时编写，与开发同步推进

## 三、用例颗粒度

- 一条用例只验证一个核心关注点
- 避免"超级用例"（一个用例覆盖N个功能点）
- 公用的前置操作抽离为公共步骤/前置条件

## 四、必须覆盖的场景类型

### 1. 正常场景
- 主流程正向验证
- 常用数据组合

### 2. 边界值
- 数值边界：最大值、最小值、0、空、null
- 长度边界：最短、最长、为空
- 集合边界：空集合、单元素、满容量

### 3. 等价类
- 有效等价类：选取代表性数据
- 无效等价类：各类非法输入

### 4. 异常场景
- 网络异常/超时
- 依赖服务不可用
- 权限不足
- 数据冲突/重复提交
- 资源耗尽（磁盘满、内存不足）

### 5. 兼容性
- 不同浏览器/设备/操作系统
- 不同版本兼容
- 数据迁移兼容

## 五、命名规范

格式：`模块_功能点_场景_预期结果`

示例：
- `用户登录_密码正确_登录成功`
- `用户登录_密码错误次数超限_账号锁定`
- `订单创建_库存不足_下单失败提示`

## 六、优先级定义

| 优先级 | 定义 | 举例 |
|--------|------|------|
| P0 | 阻断级，核心主流程，不通过不能发布 | 支付成功、登录成功 |
| P1 | 重要功能，严重影响用户使用 | 列表查询、表单提交 |
| P2 | 次要功能，不影响核心流程 | 筛选排序、导出功能 |
| P3 | 体验优化、边缘场景 | UI文案、提示语微调 |

## 七、编写要求

- 步骤描述清晰，无歧义，第三方也能照步骤执行
- 预期结果具体，不说"正常显示"，要说"显示XX内容，字体14px，颜色#333"
- 不使用"可能"、"大概"、"应该"等模糊表述
- 数据明确，不用"一些数据"、"若干条"，要说"3条数据"
